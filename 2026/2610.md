---
author: owent
categories:
  - Article
  - Blablabla
date: 2026-09-28 16:45:45
draft: true
id: 2610
tags: 
  - LLM
  - GPT
  - Claude
  - DeepSeek
  - 大语言模型
  - Transformer
  - Attention
  - MoE
  - Embedding
title: Transformer 入坑笔记（三） - Self-Attention和Transformer
type: post
---

## 背景

接上文 [《Transformer 入坑笔记（二） - 基本原理和Word Embeddings》][https://owent.net/2026/2609.html]，继续我们的 [Attention Is All You Need][1]/[Transformer][3] 学习之旅。

首先简单了解下传统的方案。

## 卷积神经网络（CNN）

卷积神经网络（CNN）似乎更适合静态数据（比如图片处理、提取特征等）。
所谓静态数据，是指每个数据组都单独和目标矩阵运算，通过卷积层、池化层、全连接层等输出。
每个数据组都单独运算所以可以大规模并发，但是数据组之间也缺乏关联。
我大概看了下原理，和我们要关注的 [Transformer][3] 关系不大，先略过了。

## 循环神经网络（RNN）

我们处理文本的时候，需要让神经网络知道上下文关系。
比如一句话: "我上午要去图书馆借书，下午去参加比赛。晚饭前我会把它还掉。"
那么在这里，需要系统要能知道，这里的“它”指的是“书”。

传统循环神经网络（RNN）的方法就是把后续数据叠加到前文输入里来传递上下文表达。
这里不详细铺开循环神经网络（RNN）的原理和过程，因为不是我们要关注的重点。大致了解一下流程和问题即可。

比如我们要翻译 I love lamas 到中文。
那么它的执行流程是先输入完整的 "I love lamas"，在把它转换成Embeddings数据集之后，每一次执行翻译一个Token。
把数据放到一个隐藏层，然后下一次执行把之前的隐藏层数据Append到下一层的输入里参与计算就好了。

| Step | Input                 | Output |
| ---- | --------------------- | ------ |
| 1    | I love lamas          | 我     |
| 2    | I love lamas **我**   | 爱     |
| 3    | I love lamas **我爱** | lamas  |

![RNN basic layout](ai-rnn-basic-layout.drawio "{max-width=640px}")

> 注意: 词向量、隐藏状态以及最终计算输出的w、b矩阵都是不同的。

显然这里有两个问题。一是每次计算都依赖上一次的结果，导致没法并发。而是如果内容太长的话会逐渐丢失前后文关系。
当然有一些方法一定程度缓解这个问题，但是也不是我们要关注的 [Transformer][3] 的重点就也略过了。

## Transformer

那前面两种模型都有各自的问题。俗话说小孩子才做选择，大人我全都要。怎么办呢？于是 [Transformer][3] 来了。

> 这有个非常好的工具 [Transformer Explainer][6] ,可以对 [Transformer][3] 里的各个流程步骤分解。有助于理解整个流程。
> 这个工具甚至还发了篇Paper[《Transformer Explainer: Learning LLM Transformers with 
Interactive Visual Explanation and Experimentation》][7] 😂。

注意力机制并不是 [《Attention Is All You Need》][1] 的首创，之前注意力机制主要用于优化传统神经网络模型的一系列问题。但是仅仅是缓解，并没有完全解决。
而新的方案则是把老的模式通通不要，只使用注意力。还是先贴 [Transformer][3] 的基本框架:

![Transformer Architecture](transformer-architecture.drawio "{max-width=640px}")

*图 1：Transformer 模型架构（来自 [《Attention Is All You Need》][1] ）。左侧为 Encoder，右侧为 Decoder，每个堆叠重复 N 次。*

### Positional Encoding

一上来，仍然是把输入数据做Embedding。具体方式和原理前面解释过了，这里就不复述了。
然后差异点是，现在融合了Positional Encoding矩阵，表示词所在的位置。

![2610-add-positional-encoding.png](2610-add-positional-encoding.png)

为什么需要Positional Encoding矩阵呢？因为 [Transformer][3] 计算注意力的时候，和词出现的先后顺序是无关的（所以才能并发），如果没有位置信息会导致计算出来的结果都一样。Positional Encoding矩阵的公式如下:

$$
\mathbf{PE}_{(\text{pos}, i)} = 
\begin{cases}
\sin\left(\frac{\text{pos}}{10000^{2i/d_{\text{model}}}}\right), & i \text{ 为偶数} \\[6pt]
\cos\left(\frac{\text{pos}}{10000^{2i/d_{\text{model}}}}\right), & i \text{ 为奇数}
\end{cases}
$$

如果序列长度为 L ，模型维度为 $d_{model}$。位置编码矩阵 $PE$ 的维度是 $[L \times d_{model}]$ 。然后:

- $pos$ ：词在序列中的位置（从 0 开始）。其中序列长度为 L，则 $0 <= pos < L$
- $d_{model}$ ：模型的总维度（如 512、768、1024 等，[Transformer][3] 论文里是 512）
- $i$ ：维度索引对（2i  和 2i+1  成对出现， $0 <= i < d_{model}$ ）。

位置公式不是定死的，只要满足几个条件即可。

- 唯一标识: 每个位置必须有独一无二的编码向量
- 相对距离可感知: 模型能轻易判断"相隔 k  个位置"的关系，而不仅是绝对位置
- 平滑过渡: 相邻位置的编码应该相近，反映位置的连续性
- 值域有界: 编码值不能过大或过小，避免破坏词嵌入的数值分布

[Transformer][3] 的方案使用的是线性函数，值域范围是 $-1 <= PE <= 1$，距离感知推导如下： 

利用三角函数加法公式：$\sin(a+b) = \sin a \cos b + \cos a \sin b$ ，令 $a = pos \cdot w_i, b = k \cdot w_i$（其中 $w_i = \frac{1}{10000^{2i/d_{\text{model}}}}$），则：

$$
\begin{aligned}
\mathbf{PE}_{(\text{pos}+k, 2i)} &= \sin(\text{pos}\cdot w_i + k\cdot w_i) \\
&= \underbrace{\sin(\text{pos}\cdot w_i)}_{\mathbf{PE}_{(\text{pos},2i)}} \underbrace{\cos(k\cdot w_i)}_{\text{仅与}k\text{有关}} + \underbrace{\cos(\text{pos}\cdot w_i)}_{\mathbf{PE}_{(\text{pos},2i+1)}} \underbrace{\sin(k\cdot w_i)}_{\text{仅与}k\text{有关}}
\end{aligned}
$$

同理: $\mathbf{PE}_{(\text{pos}+k, 2i+1)} = \mathbf{PE}_{(\text{pos},2i+1)}\cos(k\cdot w_i) - \mathbf{PE}_{(\text{pos},2i)}\sin(k\cdot w_i)$

所以是满足这些条件的。不过要注意的是，这个原始公式里，位置支持的个数为 $ 0<= pos <= 10000 \times 2\pi$ （最大的维度的波长），如果token数量更多，需要调整系数。
最后，显然对确定的总维度，这个位置编码矩阵是可以提前算好的。

### Self-Attention(自注意力机制)

[Transformer][3] 注意力机制的基本流程可以理解为一个查询过程。把 **键（K）** 和 **值（V）** 对，通过 **查询（Q）** 得到输出。
当然这些数据都是向量。可以这么理解:

- **查询（Q）** : 当前的查询的问题是什么？
- **键（K）** : 输入信息
- **值（V）** : 输入信息的特征，用于计算注意力。

实际计算时，同一份输入矩阵 $X$ 会分别乘以三组可训练权重矩阵，得到 $Q$、$K$、$V$ 三组向量。图里把维度压成 4 格只是为了画得清楚；真实模型里，$X$ 通常是 $[L, d_{model}]$，$W^Q/W^K$ 是 $[d_{model}, d_k]$，$W^V$ 是 $[d_{model}, d_v]$。

![Q/K/V 线性投影示意图](2610-qkv-projection.excalidraw "{max-width=760px}")

*图 2：Q/K/V 线性投影。Self-Attention 使用同一份输入 $X$，通过三组不同的权重矩阵生成后续注意力计算需要的 Query、Key 和 Value。*

![Scaled Dot-Product Attention And Multi-Head Attention](transformer-attention.drawio "{max-width=800px}")

*图 3：Scaled Dot-Product Attention（左）与 Multi-Head Attention（右）。左侧展示 Q/K/V 的注意力计算流程，右侧展示 h 个并行注意力头拼接后再线性投影的结构。*

对于单个单头的注意力，计算公式如下:

$$\mathrm{Attention}(Q, K, V) = \mathrm{softmax}\left(\frac{QK^T}{\sqrt{d_k}}\right)V$$

这里的 $d_k$ 是 Key 向量的维度。除以 $\sqrt{d_k}$ 是为了把点积结果拉回更合适的数值范围，避免维度变大后 softmax 太容易进入饱和区。

那 $W^Q$、$W^K$、$W^V$ 这些矩阵的也是训练出来的。

$$
\begin{aligned}
X &\in \mathbb{R}^{L \times d_{model}} \\
W^Q &\in \mathbb{R}^{d_{model} \times d_k} \\
W^K &\in \mathbb{R}^{d_{model} \times d_k} \\
W^V &\in \mathbb{R}^{d_{model} \times d_v}
\end{aligned}
$$

$$
\begin{aligned}
Q &= XW^Q \in \mathbb{R}^{L \times d_k} \\
K &= XW^K \in \mathbb{R}^{L \times d_k} \\
V &= XW^V \in \mathbb{R}^{L \times d_v}
\end{aligned}
$$

然后计算每个Q和每个Key的相似程度，也表达了每个Token对其他Token的注意层度，即: $QK^T$ 。然后这里的 $\frac{1}{\sqrt{d_k}}$ 是一个缩放，用于控制方差，让点积的方差和输入数据的维度无关（具体推导过程我就不贴了，有兴趣可以去搜一下或者自己推导一下。控制方差也能解决在float精度有限的情况下，Softmax函数 **梯度消失** 的问题。）。
最后再Softmax归一化。

![2610-self-attention-qkv.png](2610-self-attention-qkv.png)

这个图里还有一次Mask遮挡。它的作用是模拟阅读和写作过程，当看到前面的词的时候，后面的词还没看到。所以不应该参与注意力计算。

> \mathrm{softmax}\left(\frac{QK^T}{\sqrt{d_k}}\right) 的计算后结果会变成一个 L \* L 的矩阵。然后通过 \* V 把它降维到 $d_v$ 。现代化LLM有些支持1M上下文，不可能真的 1M \* 1M 这么夸张的计算量，肯定是通过稀疏注意力 / 滑动窗口 / 分块注意力或其他方式分块计算的。然后把计算量从 $O(L^2)$ 压到接近 $O(Lw)$，其中 $w$ 是窗口大小。

到这里，**单头注意力** 的主要流程就结束了。

## 大语言模型的参数量

<!-- 调查目前各个大预言模型的维度数量，层数。Word Embeddings数量级等，刨析大预言模型的参数量包含哪几部分，怎么算出来的？ -->

[1]: https://arxiv.org/pdf/1706.03762
[2]: https://www.bilibili.com/video/BV1fj6vBfEnu/
[3]: https://github.com/huggingface/transformers
[4]: https://towardsdatascience.com/nlp-illustrated-part-3-word2vec-5b2e12b6a63b/
[5]: https://aiwiki.ai/wiki/word_embedding
[6]: https://poloclub.github.io/transformer-explainer/
[7]: https://dl.acm.org/doi/pdf/10.1145/3772318.3791725
